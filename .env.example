LLM_PROVIDER=openrouter
LLM_MODEL=deepseek/deepseek-v4-flash
OPENROUTER_API_KEY=your-key-here

# MCP HTTP SSE transport (M7a)
MCP_HTTP_PORT=8766          # 任意。Tailscale Funnelに合わせて変更可。
MCP_PUBLIC_URL=https://fraine.tail204746.ts.net:8443  # OAuth issuer URL

# 日次実行上限 (M7b-2)
MCP_DAILY_QUERY_LIMIT=50    # MCP・Streamlit合算の1日あたり最大実行回数
